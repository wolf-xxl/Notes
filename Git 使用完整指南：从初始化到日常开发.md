
## 1. Git 初始化和 SSH 密钥配置

### 1.1 Git 初始配置
```bash
# 配置全局用户信息（必须设置，用于标识提交者）
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱@example.com"

# 设置默认主分支名称（避免使用旧的 master 名称）
git config --global init.defaultBranch main

# 开启颜色显示，让命令行输出更易读
git config --global color.ui auto

# 查看所有配置项，确认配置是否正确
git config --list
```

### 1.2 SSH 密钥生成与配置
```bash
# 1. 生成 SSH 密钥（使用 Ed25519 算法）
ssh-keygen -t ed25519 -C "your_email@example.com"

# 2. 启动 SSH 代理
eval "$(ssh-agent -s)"

# 3. 添加私钥到代理
ssh-add ~/.ssh/id_ed25519

# 4. 复制公钥内容
cat ~/.ssh/id_ed25519.pub

# 5. 将公钥添加到 GitHub/GitLab：
#    - GitHub: Settings → SSH and GPG keys → New SSH key
#    - GitLab: Preferences → SSH Keys

# 6. 测试连接
ssh -T git@github.com
# 成功提示：Hi username! You've successfully authenticated...
```

### 1.3 仓库初始化方法
```bash
# 方法一：本地初始化新项目
mkdir my-project          # 创建项目目录
cd my-project             # 进入目录
git init                  # 初始化 Git 仓库
git add .                 # 添加所有文件到暂存区
git commit -m "Initial commit"  # 进行首次提交

# 方法二：克隆远程仓库（使用 SSH 协议）
git clone git@github.com:username/repository.git

# 方法三：克隆到指定目录
git clone git@github.com:username/repository.git my-project-folder
```

## 2. 日常开发工作流程

### 2.1 基础开发循环
```bash
# 1. 开始工作时，先获取最新代码
git checkout main         # 切换到主分支
git pull origin main      # 拉取远程最新更改

# 2. 创建功能分支进行开发（避免直接在主分支修改）
git checkout -b feature/new-feature

# 3. 开发过程中的提交循环
#    编辑文件...
git add .                 # 将修改添加到暂存区
git commit -m "feat: 添加新功能"  # 提交更改并添加描述

# 4. 继续开发...
git add .                 # 再次添加新的修改
git commit -m "fix: 修复某个问题"  # 再次提交

# 5. 开发完成后，推送到远程仓库
git push -u origin feature/new-feature  # 首次推送需要 -u 参数
```

### 2.2 提交信息规范（推荐格式）
```bash
# 提交类型：简要描述
# 常用类型说明：
# feat:     新功能
# fix:      修复 bug
# docs:     文档更新
# style:    代码格式调整（不影响功能）
# refactor: 代码重构
# test:     测试相关
# chore:    构建过程或辅助工具变动

git commit -m "feat: 实现用户登录功能"
git commit -m "fix: 修复登录页面样式问题"
git commit -m "docs: 更新 API 使用说明"
```

## 3. 分支管理策略

### 3.1 分支基础操作
```bash
# 查看所有分支
git branch              # 查看本地分支
git branch -r           # 查看远程分支  
git branch -a           # 查看所有分支（本地+远程）

# 创建新分支（基于当前分支创建）
git branch feature/user-profile    # 创建分支但不切换
git checkout -b feature/user-profile  # 创建并切换到新分支

# 切换分支
git checkout main                  # 切换到主分支
git switch main                    # 新版本推荐命令

# 删除分支
git branch -d feature/old-branch   # 安全删除（已合并的分支）
git branch -D feature/old-branch   # 强制删除（未合并的分支）
```

### 3.2 分支合并流程
```bash
# 1. 更新主分支到最新状态
git checkout main                  # 切换到主分支
git pull origin main               # 拉取远程最新更改

# 2. 合并功能分支到主分支
git merge feature/new-feature      # 合并指定分支

# 3. 推送合并后的更改到远程
git push origin main               # 推送到远程主分支

# 4. 删除已合并的功能分支（清理）
git branch -d feature/new-feature  # 删除本地分支
git push origin --delete feature/new-feature  # 删除远程分支
```

## 4. 代码同步与更新

### 4.1 获取远程更新
```bash
# 方法一：先拉取再合并（推荐）
git fetch origin                   # 下载远程更新，但不合并
git merge origin/main              # 合并到当前分支

# 方法二：直接拉取（fetch + merge 的快捷方式）
git pull origin main               # 拉取并合并远程分支

# 方法三：变基方式更新（保持提交历史线性）
git fetch origin
git rebase origin/main             # 将本地提交"重新播放"在远程更新之上
```

### 4.2 处理合并冲突
```bash
# 当合并出现冲突时的处理流程
git merge feature/some-branch      # 执行合并，出现冲突

# 查看哪些文件有冲突
git status

# 冲突文件内容示例：
# <<<<<<< HEAD
# 当前分支的内容
# =======
# 要合并分支的内容  
# >>>>>>> feature/some-branch

# 手动编辑文件，解决冲突（删除标记，保留正确代码）
# 标记冲突已解决
git add resolved-file.txt

# 完成合并提交
git commit -m "Merge branch 'feature/some-branch'"
```

## 5. 代码撤销与回退

### 5.1 撤销工作区修改
```bash
# 撤销单个文件的未暂存修改
git restore filename.txt           # 新版本命令
git checkout -- filename.txt       # 旧版本命令

# 撤销所有未暂存的修改
git restore .                      # 恢复所有文件
git checkout -- .                  # 旧版本命令
```

### 5.2 撤销暂存区文件
```bash
# 将文件从暂存区移回工作区（取消 git add）
git restore --staged filename.txt  # 新版本命令
git reset HEAD filename.txt        # 旧版本命令
```

### 5.3 版本回退操作
```bash
# 查看提交历史，确定要回退的版本
git log --oneline                  # 简洁显示提交历史

# 回退到指定提交（保留修改）
git reset --soft commit_hash       # 回退提交，修改保留在暂存区

# 回退到指定提交（保留修改在工作区）
git reset --mixed commit_hash      # 默认方式，修改保留在工作区

# 彻底回退到指定提交（丢弃所有修改）
git reset --hard commit_hash       # 谨慎使用！会丢失所有未提交的修改

# 回退到上一个版本
git reset --hard HEAD~1            # 回退1个提交
```

## 6. 储藏和临时保存

### 6.1 储藏更改
```bash
# 临时保存当前工作区的修改
git stash                          # 储藏所有未提交的修改
git stash push -m "保存工作进度"    # 带描述信息的储藏

# 查看储藏列表
git stash list

# 恢复最近的一次储藏
git stash pop                      # 恢复并删除储藏
git stash apply                    # 恢复但不删除储藏

# 删除储藏
git stash drop stash@{0}           # 删除指定储藏
git stash clear                    # 清空所有储藏
```

## 7. 标签管理

### 7.1 创建和管理标签
```bash
# 创建轻量标签（指向特定提交的指针）
git tag v1.0.0

# 创建附注标签（包含详细信息）
git tag -a v1.0.0 -m "版本 1.0.0 发布"

# 查看所有标签
git tag

# 推送标签到远程仓库
git push origin v1.0.0             # 推送单个标签
git push origin --tags              # 推送所有标签

# 删除标签
git tag -d v1.0.0                   # 删除本地标签
git push origin --delete v1.0.0     # 删除远程标签
```

## 8. 查看历史和状态

### 8.1 查看提交历史
```bash
# 基本日志查看
git log                            # 详细提交历史
git log --oneline                  # 简洁单行显示
git log --graph                    # 图形化显示分支结构

# 查看特定文件的修改历史
git log filename.txt               # 文件的提交历史
git log -p filename.txt            # 显示文件的具体修改内容

# 查看最近 n 条提交
git log -3                         # 查看最近3条提交
```

### 8.2 查看状态和差异
```bash
# 查看当前仓库状态
git status                         # 显示工作区和暂存区状态

# 查看文件差异
git diff                           # 工作区与暂存区的差异
git diff --staged                  # 暂存区与最新提交的差异
git diff commit1 commit2           # 两个提交之间的差异
```

## 9. 完整开发流程示例

```bash
# 完整的功能开发流程示例

# 1. 准备阶段：更新主分支
echo "步骤1: 更新主分支到最新状态"
git checkout main
git pull origin main

# 2. 创建功能分支
echo "步骤2: 创建功能分支"
git checkout -b feature/user-registration

# 3. 开发阶段：多次提交
echo "步骤3: 开始开发..."
# 开发注册页面
git add .
git commit -m "feat: 添加用户注册页面布局"

# 开发表单验证
git add .
git commit -m "feat: 实现注册表单验证逻辑"

# 开发后端接口
git add .
git commit -m "feat: 添加用户注册API接口"

# 4. 同步更新：合并主分支最新更改
echo "步骤4: 合并主分支最新更新"
git fetch origin
git rebase origin/main

# 5. 推送分支
echo "步骤5: 推送功能分支到远程"
git push -u origin feature/user-registration

echo "开发完成！可以在 Git 平台创建 Pull Request"
```

## 10. 常用命令速查

### 10.1 基础命令
```bash
git init                          # 初始化新仓库
git clone <url>                   # 克隆远程仓库
git add <file>                    # 添加文件到暂存区
git commit -m "message"           # 提交更改
git status                        # 查看状态
git log                           # 查看历史
```

### 10.2 分支操作
```bash
git branch                        # 查看分支
git branch <name>                 # 创建分支
git checkout <name>               # 切换分支
git merge <branch>                # 合并分支
git branch -d <name>              # 删除分支
```

### 10.3 远程操作
```bash
git pull                          # 拉取远程更新
git push                          # 推送到远程
git fetch                         # 下载远程对象
git remote -v                     # 查看远程仓库
```

### 10.4 撤销操作
```bash
git restore <file>                # 撤销工作区修改
git restore --staged <file>       # 撤销暂存区文件
git reset --hard HEAD             # 回退到最新提交
git revert <commit>               # 创建新的提交来撤销更改
```

## 📝 使用建议和最佳实践

1. **提交频率**：小步频繁提交，每次提交只完成一个逻辑功能
2. **提交信息**：写清晰的提交信息，说明修改内容和原因
3. **分支策略**：为每个新功能或修复创建独立分支
4. **代码审查**：即使单人开发，也建议定期回顾自己的代码
5. **定期备份**：重要更改及时推送到远程仓库
6. **测试验证**：提交前进行测试，确保不会破坏现有功能
